@model IEnumerable<KomPas.Models.Podsjetnik>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tekst)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Termin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tekst)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Termin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PodsjetnikID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PodsjetnikID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PodsjetnikID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
